# GitHub Pages 部署时读取此文件
# 1) 后端 API 地址 (指向 Render 上部署的后端, 不要以 / 结尾, 不要带 /api 后缀)
VITE_API_BASE_URL=https://teaching-management-system.onrender.com

# 2) GitHub Pages 二级路径, 必须以 / 开头和结尾, 值 = /<repo-name>/
# 如果 repo 名不同请同步修改, 或用自定义域名时改为 /
VITE_BASE_URL=/teaching-management-system/
